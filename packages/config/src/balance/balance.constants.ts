export enum BalancePallet {
  Assets = 'assets',
  OrmlTokens = 'ormlTokens',
  System = 'system',
  Tokens = 'tokens',
}

export enum BalanceFunction {
  Account = 'account',
  Accounts = 'accounts',
  Asset = 'asset',
}
